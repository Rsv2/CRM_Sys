<Window x:Class="Desktop.DesktopWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Desktop" Background="{DynamicResource winColor}"
        BorderBrush="{DynamicResource fieldColor}" Foreground="{DynamicResource textColor}"
        mc:Ignorable="d"
        Title="Десктоп" Height="700" Width="880">
    <Grid>
        <Viewbox>
            <local:DesktopUI/>
        </Viewbox>
    </Grid>
</Window>
